import { 
    StyleSheet
} from "react-native";

const homeStyle = StyleSheet.create({
    buzzerToneCircular: {
        justifyContent: "center",
        alignItems: "center",
        position: 'relative',
        // width: 
        height: 290
    },
    buzzerToneDisplay: {
        position: "absolute"
    }
});

export default homeStyle;